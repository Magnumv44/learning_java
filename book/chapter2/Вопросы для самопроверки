1. Почему в Java строго определены диапазоны допустимых значений и области действия простых типов?

2. Что собой представляет символьный тип в Java и чем он отличается от символьного типа в ряде других языков программирования?

3. "Перемення типа boolean может иметь любое значение, поскольку любое ненвлевое значение интерпритируется как истинное". Верно или неверно?

4. Какая ошибка допущены в следующем фрагменте кода?

for (i = 0; i < 10; i++) {
	int sum;
	sum = sum + 1;
}
System.out.println("Сумма: " + sum);

5. Поясните различие между превиксной и постфиксной формами записи операции инкримента.

6. Покажите, каким образом укороченная логическая операция И может предотвратить деление на нуль.

7. До какомго типа повышаются типа bute и short при вычислении выражений?

8. Когда возникает потребность в явном приведении типов?

9. Определяет ли блок кода область действия переменных?

